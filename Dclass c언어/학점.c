#include <stdio.h>
main(void)
{

	//입력받은 학점이 90점 이상이면 A학점
	// 80 이상이면 B
	// 70 이상이면 C
	// 60 이상이면 D
	// 입력받은 점수가 범윙 존재하지 않은다면 재시험 당첨 출력


	{
		int score;
		printf("점수를 입력하세요: ");
		scanf("%d", &score);

		if (score >= 90 {
			printf("A학점입니다.\n");
		}

		if (score >= 80 {
			printf("B학점입니다/n");
		}

		if (score >=70 {
			printf("C학점입니다./n");
		}

		if (score >= 60 {
			printf("D학점입나다./n");
		}
		
}
	return 0;
}