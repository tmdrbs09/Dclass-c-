#include <stdio.h>
main(void)
{
	// & (and) : 대응하ㅡㄴ 각 비트의 값이 모두 1이라면 1을 반환 아니면 0
	// 	ㅣ (or) : 대응하는 비트의 값 중 1이 하나으면 1을 반환
	// ^ (xor0) : 대응하는 비트의 값이 다르면 1을 반환
	// ~ (not_) : 비트의 값이 1 이면 0으로, 0 이라면 1로 반전시켜서 나타냄

	int bit_five = 5; // 00000101
	int bit_three = 3; // 00000011
	printf("%d & %d = %d", bit_five, bit_three, bit_five & bit_three);

	int bit_seven = 7; //00000111
	printf("%d & %d", bit_seven, bit_three, bit_seven & bit_three);

	int if_number = 10;
	if (if_number == 3) {
	
	}

		//입력받은 하루 걸음수가 10000보 일때 경우매\\
		// 하루에 %d 걷다니 대단해여! 좋은 습과 입니다!
	
	int walk_count;
	printf("당신은 하루에 몇 보 걷나요?")
	char alpha; = 'A'
		if (alpha) == 'B' { 
			

			int like_number >= 10; { 
				printf("당신이 좋아하는 숫자는 10 이상이군ㅇ~~");

	return 0;
}