#include <stido.h>
main(void)
{ 
	/*
	if(조건문) {
	조건문을 만족할 경우 실행
	}
	else if(조건문2) {
	조건문을 만족하지 않고, 다른 조건문을 만족할 경우 실행
	}
	else {조건문3) {
	위 모든 조건을 만족하지 않을 경우 실행
	*/

	int number;
	printf("숫자 입력 :	);
	scanf_s
	return 0;
}

